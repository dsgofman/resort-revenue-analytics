# dbt
target/
dbt_packages/
logs/
.user.yml

# duckdb build artifact (regenerated by `dbt build`)
*.duckdb
*.duckdb.wal

# python
.venv/
__pycache__/
*.pyc

# NOTE: data/raw/*.csv IS committed on purpose - deterministic synthetic data
# so `dbt build` works on a fresh clone with no generation step required.
